# 1T Model

Purpose: planning/spec folder for the future 1T-class model.

This is not the local training target. Keep production-scale assumptions, distributed configs, cost notes, and scaling checkpoints here.

Start from `config.yaml` only after local experiments show measured gains.
