# Local Model

Purpose: small byte-level transformer for direct local training, editing, and efficiency experiments.

Run:

```bash
python -m src.training.local_trainer --config model_families/local/config.yaml
```

Use this folder for configs, checkpoints, notes, and benchmark results for local experiments.
